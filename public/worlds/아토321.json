{
  "version": 2,
  "size": {
    "rows": 8,
    "cols": 8
  },
  "robot": {
    "x": 1,
    "y": 1,
    "dir": "E",
    "tokens": 100
  },
  "id": "아토321",
  "name": "아토321",
  "difficulty": 3,
  "description": [
    "<맵: 아토321>"
  ],
  "generated": true,
  "onload": [
    "var choice, worldSize;",
    "choice = RUR.randint(1, 8);",
    "if (choice == 1) {",
    "    worldSize = 6;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 2) {",
    "    worldSize = 8;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 3) {",
    "    worldSize = 10;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 4) {",
    "    worldSize = 12;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 5) {",
    "    worldSize = 14;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 6) {",
    "    worldSize = 16;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 7) {",
    "    worldSize = 18;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "if (choice == 8) {",
    "    worldSize = 20;",
    "    RUR.set_world_size(worldSize, worldSize);",
    "}",
    "var row, xpos, xxpos;",
    "for (row=1; row<=worldSize-1; row++) {",
    "    if ((row % 2) != 0) {",
    "        for (xpos=1; xpos<=worldSize-1; xpos++) {",
    "            RUR.add_wall('north', xpos, row);",
    "        }",
    "    }",
    "    if ((row % 2) == 0) {",
    "        for (xpos=2; xpos<=worldSize; xpos++) {",
    "            RUR.add_wall('north', xpos, row);",
    "        }",
    "    }",
    "}",
    "RUR.add_object('token', 1, worldSize-1, {'number': 1});"
  ]
}
